#!/bin/bash
set -e

if getent group tcos >/dev/null; then
  delgroup --quiet --system tcos > /dev/null || true
fi

case "$1" in
    purge)
 	 # delete all generated files and dirs
 	 rm -rf /var/lib/tcos/tftp
 	 rm -rf /var/lib/tcos/files
 	 rm -rf /var/lib/tcos/fs-*
    ;;
esac



#DEBHELPER#
